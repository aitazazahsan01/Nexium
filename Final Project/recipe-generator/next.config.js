/** @type {import('next').NextConfig} */
const nextConfig = {
  // Your Next.js configuration options can go here if you have any.
  // For now, an empty object is perfect.
};

module.exports = nextConfig;
